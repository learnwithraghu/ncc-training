# Layer 1: Basic Navigation

## Goal
Move around the Linux file system using common navigation commands.

## Tasks
1. Run `pwd`.
2. List the files in the current directory with `ls`.
3. Show hidden files with `ls -a`.
4. Change to your home directory with `cd ~`.
5. Change to the root directory with `cd /`.
6. Return to your previous directory with `cd -`.
