# Layer 3: Basic Navigation Solution

## Commands
```bash
pwd
ls
ls -a
cd ~
cd /
cd -
```

## Notes
- `~` means the home directory.
- `/` means the root directory.
- `cd -` switches back to the previous directory.