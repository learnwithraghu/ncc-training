# Layer 1: Directory Structure

## Goal
Practice working with absolute and relative paths.

## Tasks
1. Create a directory named `sandbox`.
2. Inside `sandbox`, create a subdirectory named `notes`.
3. Move into `notes` using a relative path.
4. Return to your home directory using an absolute path.
