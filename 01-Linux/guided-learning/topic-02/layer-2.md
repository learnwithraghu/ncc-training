# Layer 2: Directory Structure Checkpoint

## What to Notice

- `mkdir -p` creates nested directories in one step.
- Relative paths depend on your current location.
- Absolute paths start from `/`.

## Quick Check
Show the command you would use to create `sandbox/notes` in one step.