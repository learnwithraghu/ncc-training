# Layer 3: Directory Structure Solution

## Commands
```bash
mkdir -p sandbox/notes
cd sandbox/notes
cd ~
```

## Notes
- Use `mkdir -p` when you want to create a path with intermediate folders.
- Relative paths are evaluated from the current directory.