# Layer 2: Help and Man Pages Checkpoint

## What to Notice

- `man` shows the full manual page.
- `--help` gives a short command summary.
- In `man`, search with `/term` and press `n` for the next match.

## Quick Check
Which command would you use for a short summary: `man` or `--help`?