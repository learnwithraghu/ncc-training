# Layer 3: Help and Man Pages Solution

## Commands
```bash
man ls
/hidden
mkdir --help
man chmod
```

## Notes
- `man` shows the full manual page.
- `--help` gives a shorter overview.
- In `man`, search with `/term` and press `n` for the next result.