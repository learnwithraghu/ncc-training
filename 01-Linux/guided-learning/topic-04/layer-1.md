# Layer 1: Hidden Files and Globbing

## Goal
Practice hidden files and simple wildcard matching.

## Tasks
1. Create a hidden file named `.env`.
2. Create two text files named `alpha.txt` and `beta.txt`.
3. List only the `.txt` files.
4. Show hidden files in the directory.
