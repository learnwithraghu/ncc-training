# Layer 2: Hidden Files and Globbing Checkpoint

## What to Notice

- Files starting with `.` are hidden from a normal `ls`.
- `*.txt` matches every file ending in `.txt`.

## Quick Check
Why does `ls` not show `.env` by default?