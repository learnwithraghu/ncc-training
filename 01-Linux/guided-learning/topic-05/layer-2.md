# Layer 2: File Management Checkpoint

## What to Notice

- `mkdir` creates folders.
- `touch` creates empty files.
- `cp` copies files.
- `mv` renames or moves files.
- `rm` deletes files immediately.

## Quick Check
Which command would you use to rename `todo.txt` to `tasks.txt`?