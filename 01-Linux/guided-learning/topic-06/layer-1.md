# Layer 1: File Permissions

## Goal
Practice reading and changing permissions.

## Tasks
1. Create a file named `script.sh`.
2. Check its current permissions with `ls -l`.
3. Make the file executable for the owner.
4. Confirm the permission change.
