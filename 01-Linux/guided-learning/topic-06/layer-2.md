# Layer 2: File Permissions Checkpoint

## What to Notice

- `ls -l` shows permission bits.
- `chmod u+x` adds execute permission for the owner.
- The `x` bit is needed to run a script.

## Quick Check
What does the `x` permission allow you to do?