# Layer 3: File Permissions Solution

## Commands
```bash
touch script.sh
ls -l script.sh
chmod u+x script.sh
ls -l script.sh
```

## Notes
- `u+x` adds execute permission for the owner.
- `ls -l` is the easiest way to verify the change.