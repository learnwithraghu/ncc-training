# Layer 1: Editing with Nano and Vim

## Goal
Practice editing a simple text file with two different editors.

## Tasks
1. Create a file named `notes.txt`.
2. Open it with `nano` and add one line of text.
3. Open it with `vim` and add another line.
4. Save the file and confirm the contents.
