# Layer 2: Editing with Nano and Vim Checkpoint

## What to Notice

- `nano` is simple and beginner-friendly.
- `vim` uses insert mode and command mode.
- In `vim`, press `i` to insert, `Esc` to exit insert mode, and `:wq` to save and quit.

## Quick Check
What do you press in `vim` before typing text?