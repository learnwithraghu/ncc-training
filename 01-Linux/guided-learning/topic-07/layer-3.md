# Layer 3: Editing with Nano and Vim Solution

## Example Flow
```bash
touch notes.txt
nano notes.txt
vim notes.txt
cat notes.txt
```

## Notes
- Use the on-screen shortcuts in `nano`.
- In `vim`, use `i`, `Esc`, and `:wq`.