# Layer 2: Text Editor Practice Checkpoint

## What to Notice

- `echo` with `>` creates or replaces file content.
- `echo` with `>>` appends to the end of a file.
- `cat` is a quick way to view file contents.

## Quick Check
What is the difference between `>` and `>>`?