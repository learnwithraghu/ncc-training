# Layer 1: Process Management

## Goal
Practice finding and stopping processes.

## Tasks
1. Start a background process with `sleep 300 &`.
2. Find its process ID with `ps` or `pgrep`.
3. View the process details.
4. Stop the process with `kill`.
