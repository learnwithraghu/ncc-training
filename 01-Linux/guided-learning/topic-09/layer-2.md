# Layer 2: Process Management Checkpoint

## What to Notice

- A process ID (PID) identifies a running process.
- `ps` and `pgrep` help you find the PID.
- `kill` sends a signal to the process.

## Quick Check
Why do you need the PID before using `kill`?