# Layer 3: Process Management Solution

## Commands
```bash
sleep 300 &
ps aux | grep sleep
kill <PID>
```

## Notes
- Use a background sleep process for a safe demo.
- Replace `<PID>` with the actual process ID.