# Layer 1: Log Troubleshooting

## Goal
Practice locating and reading system logs.

## Tasks
1. Look for logs under `/var/log`.
2. Open one log file and inspect the last few lines.
3. Search the log for a keyword such as `error` or `fail`.
4. Record one command you would use to keep watching a log file.
