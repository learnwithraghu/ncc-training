# Layer 2: Log Troubleshooting Checkpoint

## What to Notice

- `tail -n` shows the latest lines.
- `grep` searches for text inside a file.
- `tail -f` follows a file as it grows.

## Quick Check
Which command would you use if you want to watch a log live?