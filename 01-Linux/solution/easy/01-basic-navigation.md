# Solution: Basic Navigation

## Commands
```bash
pwd
ls
ls -a
cd ~
cd /
cd -
```

## Notes
- `~` means the current user's home directory.
- `/` means the root of the file system.
- `cd -` switches back to the previous directory.