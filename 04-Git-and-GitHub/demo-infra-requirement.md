# Demo Infra Requirement

## Infra Needed
- Git client
- GitHub account
- Optional: GitHub CLI for easier validation

## Quick Validation
```bash
git --version
git config --get user.name && git config --get user.email
command -v gh && gh auth status || echo 'gh not installed or not authenticated yet'
```
