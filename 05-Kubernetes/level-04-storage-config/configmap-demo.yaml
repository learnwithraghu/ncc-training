apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  ENV: "production"
  LOG_LEVEL: "info"
  DB_HOST: "mysql.default.svc.cluster.local"
  app.properties: |
    database_url=mysql://db:3306
    cache_enabled=true
    max_connections=100
