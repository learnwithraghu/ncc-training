# Demo Infra Requirement

## Infra Needed
- Git and GitHub repository access
- Ability to push branches and open pull requests
- Optional: GitHub CLI

## Quick Validation
```bash
git --version
git remote -v
command -v gh && gh auth status || echo 'gh not installed or not authenticated yet'
```
