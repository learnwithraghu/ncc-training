apiVersion: v1
kind: Service
metadata:
  name: orbital-relay
  labels:
    app: orbital-relay
spec:
  type: NodePort
  selector:
    app: orbital-relay
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
