apiVersion: v1
kind: Service
metadata:
  name: {{ include "document-search.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "document-search.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "document-search.labels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      name: http
