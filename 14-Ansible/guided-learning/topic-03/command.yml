---
- name: Run ad-hoc style commands from a playbook
  hosts: web
  gather_facts: false
  tasks:
    - name: Show hostname
      ansible.builtin.command: hostname
      register: hostname_out
      changed_when: false

    - name: Print hostname
      ansible.builtin.debug:
        var: hostname_out.stdout
