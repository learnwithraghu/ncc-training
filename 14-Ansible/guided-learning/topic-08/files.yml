---
- name: Manage files and permissions
  hosts: web
  become: true
  tasks:
    - name: Create a config file
      ansible.builtin.copy:
        dest: /tmp/lab-config.txt
        content: "hello from ansible\n"
        owner: root
        group: root
        mode: "0644"
