apiVersion: v1
kind: Service
metadata:
  name: daypack
  namespace: daypack
  labels:
    app: daypack
spec:
  type: ClusterIP
  selector:
    app: daypack
  ports:
    - name: http
      protocol: TCP
      port: 8501
      targetPort: 8501
